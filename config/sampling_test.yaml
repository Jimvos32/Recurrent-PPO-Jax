tags: null
project_name: "batch_test_seeded"
run_name: "batch_test_seeded"
seed: 55
steps: 500000
log_interval: 20000
eval_episodes: 1
eval_interval: 20000
use_wandb: true
defaults:
  - _self_
  - task: masked
  - trainer: ppo
